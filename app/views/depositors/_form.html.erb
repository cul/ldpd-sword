<%= form_for(@depositor) do |f| %>
  <% if @depositor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@depositor.errors.count, "error") %> prohibited this depositor from being saved:</h2>

      <ul>
      <% @depositor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
