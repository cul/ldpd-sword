module Sword
module Composers
class HyacinthComposer

  # takes a DepositContent that was populated via one of the parsers
  # development heuristic: for now, will handle all attributes in DepositContent
  def compose(deposit_content)

  end
end
end
end
